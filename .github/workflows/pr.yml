name: PR Checks

on:
  pull_request:
    paths:
      - packages/create-cloudflare/**

env:
  node-version: 16.14

jobs:
  check:
    name: "Checks"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: test
        run: echo "this is a test"

      - name: test failure
        run: exit 1
